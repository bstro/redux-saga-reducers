{
  "name": "redux-saga-reducers",
  "version": "0.0.4",
  "description": "the power of reducers, sagas, and generators combined",
  "main": "./distribution/index.js",
  "scripts": {
    "test": "jest --verbose",
    "prepublish": "npm run build",
    "build": "babel source --plugins babel-plugin-transform-runtime --presets babel-preset-es2015 --out-dir distribution"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "redux-saga": "^0.12.0"
  },
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-core": "^6.22.1",
    "babel-jest": "^18.0.0",
    "babel-plugin-transform-runtime": "^6.22.0",
    "babel-polyfill": "^6.22.0",
    "babel-preset-es2015": "^6.22.0",
    "jest-cli": "^18.1.0"
  }
}
