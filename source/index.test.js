import createSagaStore from './index.js';
import { nextify } from './test-helpers.js';

// describe("createSagaStore", () => {
//   const sagaReducer = { foo: i => i }
//   const initialState = 'initial state';
//   const rootSaga = 'saga';
//   const middleware = state => next => action => state;

//   it("puts the sagaMiddleware at the front of the middleware array", () => {
//     const result = createSagaStore(sagaReducer, initialState, rootSaga)
//   });
// })

it("puts the lotion on the skin", () => {

});